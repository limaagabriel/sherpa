# sherpa adversarial-builder subagent — Codex role binding.
# Full role in plugin file agents/adversarial-builder.md; this TOML binds the
# model tier + sandbox. Tier: standard build (Claude: sonnet).
name = "adversarial-builder"
description = '''Implements a pre-vetted briefing: search, edits, build/test, and a reviewer-ready handoff. Commits once per subtask carrying a Build-Id note; returns handoff path + build verdict; never pushes.'''
model = "gpt-5.5"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = '''
You are sherpa's adversarial-builder subagent. Read your full role definition,
invariants, and output contract from the sherpa plugin file
agents/adversarial-builder.md (resolve via $CLAUDE_PLUGIN_ROOT when set, else the
installed sherpa plugin root) and follow it exactly. Implement the vetted
briefing, land one Build-Id-noted commit per subtask, never push. Your final
message IS the return value (handoff path + build verdict), not a human-facing note.
'''
